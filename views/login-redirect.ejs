<!DOCTYPE html>
<html :class="{ 'theme-dark': dark }" x-data="data()" lang="en">
<%- include('head') %>

    <body>
        <div class="flex h-screen bg-gray-50 dark:bg-gray-900" :class="{ 'overflow-hidden': isSideMenuOpen}">
            <div class="flex flex-col flex-1">
                <main class="h-full pb-16 overflow-y-auto">
                    <div class="container flex flex-col items-center px-6 mx-auto">
                        <p class="text-xl font-semibold text-gray-700 dark:text-gray-200">
                            Succussfully logged in. Redirecting in 5 secs
                        </p>
                        <p class="text-gray-700 dark:text-gray-300">
                            <a class="text-purple-600 hover:underline dark:text-purple-300" href="/">
                                Click this link if page does not redirect automatically.
                            </a>
                        </p>
                    </div>
                </main>
            </div>
        </div>
        <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/alpine.min.js"></script>
        <script>
            setTimeout(() => { document.location.href = "/" }, 5000)
        </script>
    </body>

</html>